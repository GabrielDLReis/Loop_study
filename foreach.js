const japones = ['japonês', 'chinês', 'asiático', 'tailandês', 'chingling']

japones.forEach((value, index, array) => {
  console.log(`${index + 1}. ${value}`)
  // Index nos dará o número da array, value nos dará o que está escrito e a array mostrará tudo que está escrito por array.
})
